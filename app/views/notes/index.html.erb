<div class = "container">

  <h1> All notes by All users </h1>

      <% @notes.each do |note| %>
      <p>
        <%= note.id %>.
        <%= note.title %>:
        <%= note.body %>
      </p>
      <% end %>
      <%= link_to "Sign in", new_user_session_path, method: :get %>
</div>
