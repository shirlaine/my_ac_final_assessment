<div class = "container">

  <h1> Edit note <%= @note.id %></h1>

   <%= simple_form_for @note, url: note_path(@note), method: :put do |f| %>
     <%= f.input :title %>
     <%= f.input :body %>
     <%= f.submit %>
   <% end %>

</div>
